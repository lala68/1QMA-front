import { ShamsiDatePipe } from './shamsi-date.pipe';

describe('ShamsiDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ShamsiDatePipe();
    expect(pipe).toBeTruthy();
  });
});
